test Action {

	generate PhpUnit;

	depend Person;

	GIVEN:
	 testGenerator_junit_testcase_action.ump;
	WHEN:
	 Person p1 ("John", 123) ;
	THEN:

	test someTest {
	 Person p1 ("John", "123","someAddrss" );
	 p1.setName("Jane");
	 p1.setId(321);
	 assertTrue(p1.getId() != null);
	 }
}
